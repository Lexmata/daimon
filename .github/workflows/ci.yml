name: CI

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -Dwarnings

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  # ── Fast checks (no compilation) ──────────────────────────────────
  fmt:
    name: Formatting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --all --check

  commitlint:
    name: Commit messages
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install cargo-commitlint
        run: cargo install cargo-commitlint --locked
      - name: Lint commits
        run: |
          for sha in $(git log --format=%H origin/${{ github.base_ref }}..HEAD); do
            git log -1 --format=%s "$sha" | cargo commitlint check --config commitlint.toml
          done

  # ── Compilation matrix ────────────────────────────────────────────
  check:
    name: Check (${{ matrix.toolchain }}, ${{ matrix.features }})
    runs-on: ubuntu-latest
    needs: [fmt]
    strategy:
      fail-fast: false
      matrix:
        toolchain: [stable, "1.95"]
        features:
          - "--no-default-features"
          - "--features openai"
          - "--features anthropic"
          - "--features bedrock"
          - "--features full"
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.toolchain }}
      - uses: Swatinem/rust-cache@v2
        with:
          key: check-${{ matrix.toolchain }}-${{ matrix.features }}
      - name: Install protoc
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler
      - run: cargo check ${{ matrix.features }}

  # ── Feature-flag isolation ────────────────────────────────────────
  # Compiles every feature of the root crate in isolation so a feature
  # that only builds when another happens to be enabled can't slip in.
  hack:
    name: Feature matrix (cargo hack)
    runs-on: ubuntu-latest
    needs: [fmt]
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          key: hack
      - name: Install protoc
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler
      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-hack
      - run: cargo hack check --each-feature --no-dev-deps -p daimon

  # ── Linting ───────────────────────────────────────────────────────
  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    needs: [check]
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
        with:
          key: clippy
      - name: Install protoc
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler
      - name: Clippy (no default features)
        run: cargo clippy --workspace --no-default-features --all-targets -- -D warnings
      - name: Clippy (full features)
        run: cargo clippy --workspace --features full --all-targets -- -D warnings

  # ── Tests ─────────────────────────────────────────────────────────
  test:
    name: Test (${{ matrix.features }})
    runs-on: ubuntu-latest
    needs: [check]
    strategy:
      fail-fast: false
      matrix:
        features:
          - "--no-default-features"
          - "--features full"
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          key: test-${{ matrix.features }}
      - name: Install protoc
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler
      - run: cargo test --workspace ${{ matrix.features }} -- --nocapture

  # ── Coverage ──────────────────────────────────────────────────────
  coverage:
    name: Coverage (≥80%, full features)
    runs-on: ubuntu-latest
    needs: [test]
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview
      - uses: Swatinem/rust-cache@v2
        with:
          key: coverage
      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-llvm-cov
      - name: Install protoc
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler
      # Full features so feature-gated code (mcp, http-server, distributed
      # backends, providers) is inside the gate; the previous 84% gate
      # measured --no-default-features only. Full-feature baseline: 81.2%.
      - name: Generate coverage
        run: cargo llvm-cov --features full --fail-under-lines 80 --lcov --output-path lcov.info
      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-lcov
          path: lcov.info
          retention-days: 7

  # ── Examples ──────────────────────────────────────────────────────
  examples:
    name: Examples
    runs-on: ubuntu-latest
    needs: [check]
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          key: examples
      - name: Install protoc
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler
      - name: Check all examples
        run: cargo check --examples --features full

  # ── Documentation ─────────────────────────────────────────────────
  docs:
    name: Documentation
    runs-on: ubuntu-latest
    needs: [check]
    env:
      RUSTDOCFLAGS: -Dwarnings
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          key: docs
      - name: Install protoc
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler
      - run: cargo doc --workspace --features full --no-deps --document-private-items

  # ── Dependency audit ──────────────────────────────────────────────
  deny:
    name: cargo-deny
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: EmbarkStudios/cargo-deny-action@v2
        with:
          command: check advisories licenses bans

  # ── Final gate ────────────────────────────────────────────────────
  ci-pass:
    name: CI passed
    if: always()
    needs: [fmt, check, hack, clippy, test, coverage, examples, docs, deny]
    runs-on: ubuntu-latest
    steps:
      - name: Verify all jobs succeeded
        run: |
          results=( \
            "${{ needs.fmt.result }}" \
            "${{ needs.check.result }}" \
            "${{ needs.hack.result }}" \
            "${{ needs.clippy.result }}" \
            "${{ needs.test.result }}" \
            "${{ needs.coverage.result }}" \
            "${{ needs.examples.result }}" \
            "${{ needs.docs.result }}" \
            "${{ needs.deny.result }}" \
          )
          for r in "${results[@]}"; do
            if [[ "$r" != "success" ]]; then
              echo "::error::One or more CI jobs failed or were cancelled."
              exit 1
            fi
          done
          echo "All CI jobs passed."
